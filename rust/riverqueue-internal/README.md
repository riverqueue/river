# riverqueue-internal

Exact-version implementation details shared by River's Rust crates and River
Pro. This crate is published only as required dependency plumbing. Its API is
explicitly unstable; applications should depend on `riverqueue` instead.
